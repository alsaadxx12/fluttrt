# نشر تحديث لمستخدمي CINEBALL

## الأمر الوحيد

بعد أي تعديل على التطبيق، نفّذه من مجلد المشروع:

```
python tool/release.py --notes "وصف التحديث الذي يراه المستخدم" --deploy
```

انتهى. يصل التحديث لكل مستخدم خلال ثوانٍ من فتحه التطبيق.

`--notes` هو النص الذي يظهر للمستخدم داخل نافذة التحديث، فاكتبه بلغة يفهمها،
مثل "إصلاح مشكلة في مشغّل المباريات" لا "fix null check in player".

---

## ماذا يفعل الأمر

| # | الخطوة |
|---|---|
| 1 | يرفع رقم الإصدار في `pubspec.yaml` (1.0.4+6 تصير 1.0.5+7) |
| 2 | يبني نسخة الإصدار موقّعة بمفتاحك |
| 3 | يتحقق أن الـAPK يحمل الرقم الجديد، وأنه غير موقّع بمفتاح debug، وأن رابط التحديث بداخله صحيح |
| 4 | ينسخ الـAPK ويكتب `version.json` ببصمة SHA-256 وحجم جديدين |
| 5 | ينشر الموقع على Netlify |

إن فشل أي فحص في الخطوة 3 توقّف الأمر ولم ينشر شيئاً. هذه الفحوص تمنع أسوأ
ثلاثة أخطاء: نشر نسخة برقم قديم، أو بمفتاح خاطئ، أو تشير إلى رابط خاطئ.

---

## خيارات تحتاجها أحياناً

| الخيار | متى |
|---|---|
| `--minor` | ميزة كبيرة: 1.0.4 تصير 1.1.0 بدل 1.0.5 |
| `--major` | تغيير جذري: تصير 2.0.0 |
| `--force` | إصلاح حرج: يُمنع استخدام التطبيق قبل التحديث |
| `--min 6` | إيقاف كل النسخ الأقدم من 6 عن العمل |
| `--disable` | نشر بلا إشعار تحديث |
| `--dry-run` | معاينة الأرقام دون بناء أو نشر |

مثال على تحديث إجباري:

```
python tool/release.py --notes "إصلاح ضروري" --force --deploy
```

---

## إن اكتشفت عطلاً في نسخة نشرتها

لا تحتاج بناءً جديداً لإيقاف الإشعار. عدّل `updateEnabled` إلى `false` في
`netlify/public/download/version.json` ثم:

```
netlify deploy --prod --dir=netlify/public
```

يتوقف إشعار التحديث عن كل المستخدمين فوراً. أعده إلى `true` بعد الإصلاح.

---

## ثلاثة أمور ممنوعة

1. **تغيير اسم الحزمة** `com.antigravity.yt.youtube_downloader`. تغييره يجعل
   أندرويد يعتبر التحديث تطبيقاً مختلفاً.
2. **التوقيع بمفتاح آخر** غير `android/cineball-release.jks`. النسخة الموقّعة
   بمفتاح مختلف لا تُثبَّت فوق القديمة أبداً.
3. **نشر رقم إصدار مساوٍ أو أصغر** من المنشور. الرقم بعد `+` يجب أن يكبر دائماً.

كسر أي منها يعني أن كل مستخدم يجب أن يحذف التطبيق ويثبّته يدوياً من جديد.

---

## ملفات لا يجوز فقدانها

| الملف | لماذا |
|---|---|
| `android/cineball-release.jks` | مفتاح التوقيع. فقدانه يعني نهاية التحديثات لكل المستخدمين |
| `android/key.properties` | كلمات مرور المفتاح |

احتفظ بنسخة منهما خارج الجهاز. الملفان مستثنيان من git عمداً.

---

## روابط

- صفحة التحميل للمستخدمين الجدد: https://cineball.netlify.app/download/
- ملف الإصدار الذي يقرأه التطبيق: https://cineball.netlify.app/download/version.json
