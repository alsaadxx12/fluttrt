# Connected Mobile Device Sync Rule

- The user has an Android physical device connected via USB (SM-A266B).
- After any code modification, always ensure that Hot Reload (`r`) or Hot Restart (`R`) is sent to the running debug session on the phone so the user can immediately preview changes.
- If the debug session terminates, re-run `flutter run -d <deviceId>` immediately to keep live preview active.
